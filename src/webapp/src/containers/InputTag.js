import { connect } from 'react-redux';
import InputTagComponent from '../components/InputTag';

const mapStateToProps = (state) => {
    return {
    };
};

const mapDispatchToProps = (dispatch) => {
    return {
    };
};

const InputTag = connect(
    mapStateToProps,
    mapDispatchToProps
)(InputTagComponent);

export default InputTag;